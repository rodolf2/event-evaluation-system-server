# Minimal requirements for Railway (avoiding memory-heavy scipy)
textblob>=0.18.0
langid>=1.1.6
# Note: We explicitly don't include scipy/numpy/pandas to stay under memory limits
# Install nltk without scipy by using: pip install nltk --no-deps